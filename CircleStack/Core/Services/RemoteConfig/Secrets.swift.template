//
//  Secrets.swift
//  CircleStack
//
//  ⚠️  NOT IN GIT — see Secrets.template.swift
//
//  This file is listed in .gitignore. Copy Secrets.template.swift to Secrets.swift
//  and fill in the real value to build. The same value must be set on the Worker:
//
//      npx wrangler secret put APP_SECRET
//

enum Secrets {
    /// Shared with the Cloudflare Worker's APP_SECRET.
    static let remoteConfig = "REPLACE_WITH_YOUR_SECRET"
}
